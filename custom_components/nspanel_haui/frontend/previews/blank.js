/**
 * NSPanel HAUI - Editor - Panel preview: Blank.
 */
import { html } from '../lit-import.js';

export function renderBlankPreview(_host, _panel, _pIdx, _pt) {
  return {
    content: html``,
  };
}
