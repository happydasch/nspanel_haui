# Settings Panel

```yaml
key: sys_settings
type: system_settings
```

This internal panel is being used to provide settings on the device.

Available settings:

- full brightness
- dimmed brightness

## Config

`
`
